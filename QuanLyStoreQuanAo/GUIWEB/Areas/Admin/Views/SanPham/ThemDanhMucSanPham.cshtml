@model DTO.DanhMucSanPham
@{
    ViewBag.Title = "Thêm Danh Mục Sản Phẩm Mới";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPage1_Admin.cshtml";
}

<div class="card">
    <div class="card-body" style="margin-top: 60px">
        <h2 class="my-4 text-center">Thêm Danh Mục Sản Phẩm</h2>

        @using (Html.BeginForm("ThemDanhMucSanPham", "SanPham", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            @Html.AntiForgeryToken()
            <div class="row">
                <div class="col-md-8 offset-md-2">

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.TenDanhMuc, "Họ tên:", htmlAttributes: new { @class = "form-label" })
                        @Html.TextBoxFor(m => m.TenDanhMuc, new { @class = "form-control", @placeholder = "Nhập tên danh mục", required = "required" })
                        @Html.ValidationMessageFor(m => m.TenDanhMuc, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group mb-3">
                        @Html.LabelFor(m => m.MoTa, "Mô tả:", htmlAttributes: new { @class = "form-label" })
                        @Html.TextBoxFor(m => m.MoTa, new { @class = "form-control", @placeholder = "Nhập mô tả", required = "required" })
                        @Html.ValidationMessageFor(m => m.MoTa, "", new { @class = "text-danger" })
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary btn-block">Thêm Danh Mục Sản Phẩm</button>
                        <a href="@Url.Action("DanhMucSanPham", "SanPham")" class="btn btn-secondary mt-2">Quay lại</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>